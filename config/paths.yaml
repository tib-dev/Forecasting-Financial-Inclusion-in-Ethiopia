paths:
  data:
    raw: "data/raw"
    staged: "data/staged"
    curated: "data/curated"
    reference: "data/reference"

  reports:
    figures: "reports/figures"
    logs: "reports/logs"
    docs: "reports/docs"

  src:
    root: "src"
    modules: "src/fi_forecasting"

  tests:
    root: "tests"
    unit: "tests/unit"
    integration: "tests/integration"

  scripts:
    root: "scripts"
    pipeline: "scripts/pipeline"

  config:
    root: "config"
